<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <title>Replays - Carnaval</title>
    <style>
      :root {
        --cor-fundo: #f0f4f8;
        --cor-card: #ffffff;
        --cor-texto: #1f2937;
        --cor-sombra: rgba(0, 0, 0, 0.1);
        --cor-borda-hover: #4a90e2;
      }

      html,
      body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
        background-color: var(--cor-fundo);
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Helvetica, Arial, sans-serif;
        box-sizing: border-box;
      }

      *,
      *:before,
      *:after {
        box-sizing: inherit;
      }

      .container {
        width: 100%;
        max-width: 900px;
        margin: 0 auto;
        padding: 1.5rem 1rem;
      }

      h1 {
        font-size: 2rem;
        font-weight: 700;
        color: var(--cor-texto);
        text-align: center;
        margin-top: 0;
        margin-bottom: 2rem;
      }

      /* Grid dos cards */
      .replay-grid {
        display: grid;
        /* Duas colunas de tamanho igual, com espaço entre elas */
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem; /* Espaçamento entre os cards */
      }

      /* Estilo do card individual */
      .replay-link {
        display: flex;
        flex-direction: column;
        align-items: center;

        background-color: var(--cor-card);
        border-radius: 0.75rem; /* 12px */
        box-shadow: 0 10px 15px -3px var(--cor-sombra),
          0 4px 6px -2px var(--cor-sombra);
        padding: 1.5rem;
        text-decoration: none;

        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        border: 2px solid transparent;

        cursor: pointer;
      }

      .replay-link:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 25px -5px var(--cor-sombra),
          0 10px 10px -5px var(--cor-sombra);
        border-color: var(--cor-borda-hover);
      }

      .replay-link-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--cor-texto);
        text-align: center;
        margin-bottom: 1rem;
      }

      .replay-link-color {
        width: 100%;
        max-width: 160px;
        height: auto;
        aspect-ratio: 1/1;
        object-fit: cover;
        border-radius: 0.5rem;
        background-color: #eee;
      }

      /* Para telas menores (celulares), uma coluna só */
      @media (max-width: 600px) {
        .replay-grid {
          grid-template-columns: 1fr;
        }
        h1 {
          font-size: 1.5rem;
        }
        .replay-link {
          padding: 1.5rem 1rem;
        }
        .replay-link-title {
          font-size: 1.1rem;
        }
        .replay-link-color {
          max-width: 120px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Qual parte você gostaria de rever?</h1>

      <div
        class="replay-grid"
      >
        <a class="replay-link" href="/webmedia/companion/replay/comissao">
          <span class="replay-link-title">Comissão de Frente</span>
          <img
            class="vote-button-image"
            src="/media/webmedia/icon_arrow1.png"
            alt="Red Button"
          />
        </a>
        <a class="replay-link" href="/webmedia/companion/replay/abrealas">
          <span class="replay-link-title">Abre-alas</span>
          <img
            class="vote-button-image"
            src="/media/webmedia/icon_arrow2.png"
            alt="Green Button"
          />
        </a>
        <a class="replay-link" href="/webmedia/companion/replay/mestresala">
          <span class="replay-link-title">Mestre Sala e Porta Bandeira</span>
          <img
            class="vote-button-image"
            src="/media/webmedia/icon_arrow3.png"
            alt="Blue Button"
          />
        </a>
      </div>
    </div>
  </body>
</html>
