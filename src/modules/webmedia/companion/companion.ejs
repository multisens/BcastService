<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <!-- Essencial para design responsivo e para evitar zoom em celulares -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Receptor de Mensagem</title>
    
    <!-- Links para Google Fonts removidos -->

    <style>
        :root {
            /* Parâmetro da cor de fundo:
              Altere este valor hexadecimal para mudar a cor de fundo da página.
              Ex: #f0f4f8 (azul claro), #fef9c3 (amarelo claro), #f3e8ff (roxo claro)
            */
            --cor-fundo: #f0f4f8;
        }

        /* Configurações globais para desativar a rolagem (scroll)
          e aplicar a cor de fundo parametrizada.
        */
        html, body {
            height: 100%;
            width: 100%;
            margin: 0;
            padding: 0;
            overflow: hidden; /* Impede a rolagem */
            background-color: var(--cor-fundo);
            /* Atualizado para usar fontes padrão do sistema */
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
            
            /* Centraliza o card na tela */
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        /* Estilização do card principal */
        .card {
            width: 100%;
            max-width: 32rem; /* 512px */
            background-color: white;
            border-radius: 0.75rem; /* 12px */
            /* Sombra sutil, equivalente ao shadow-2xl do Tailwind */
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            padding: 1.5rem; /* 24px */
            margin: 1rem; /* 16px - p-4 do body anterior */
            box-sizing: border-box; /* Garante que o padding não estoure o width */
        }

        /* Título */
        h1 {
            font-size: 1.5rem; /* 24px */
            font-weight: 700;
            color: #1f2937; /* text-gray-800 */
            margin-top: 0;
            margin-bottom: 1rem; /* 16px */
            text-align: center;
        }

        /* Parágrafo de descrição */
        p {
            font-size: 0.875rem; /* 14px */
            color: #6b7280; /* text-gray-500 */
            margin-top: 0;
            margin-bottom: 1rem; /* 16px */
            text-align: center;
            line-height: 1.5;
        }
        
        /* Estiliza o `code` inline */
        p code {
            background-color: #f3f4f6; /* bg-gray-100 */
            padding: 2px 5px;
            border-radius: 4px;
            font-family: monospace;
            font-size: 0.85em;
        }

        /* Área de pré-formatação (pre) onde o JSON será exibido */
        #message-content {
            background-color: #111827; /* bg-gray-900 */
            color: #4ade80; /* text-green-400 */
            padding: 1rem; /* 16px */
            border-radius: 0.5rem; /* 8px */
            font-size: 0.875rem; /* 14px - aumentado de text-xs para melhor leitura */
            font-family: monospace;
            
            /* Controle de rolagem e quebra de linha */
            overflow: auto; /* Adiciona rolagem interna se o conteúdo for grande */
            white-space: pre-wrap; /* Mantém a formatação do JSON */
            word-wrap: break-word; /* Quebra palavras longas */
            max-height: 20rem; /* 320px - max-h-80 */
        }

    </style>
</head>
<body>

    <!-- 
      Card principal onde o conteúdo da mensagem será exibido.
      A estilização agora é feita via CSS puro.
    -->
    <div class="card">
        <h1>
            Mensagem Recebida
        </h1>
        
        <p>
            O conteúdo recebido via <code>postMessage</code> será exibido abaixo:
        </p>

        <!-- 
          Área de pré-formatação (pre) onde o JSON será exibido.
        -->
        <pre id="message-content">Aguardando mensagem...</pre>
    </div>

    <script>
        // Adiciona o listener para o evento 'message'
        window.addEventListener('message', (event) => {
            console.log('Mensagem recebida:', event.data);

            const messageContainer = document.getElementById('message-content');
            
            if (messageContainer) {
                try {
                    // Tenta formatar a mensagem como um JSON legível (com 2 espaços de indentação)
                    const formattedJson = JSON.stringify(event.data, null, 2);
                    messageContainer.textContent = formattedJson;
                } catch (error) {
                    // Se não for um JSON ou houver erro, exibe como texto simples
                    console.error('Erro ao stringificar a mensagem:', error);
                    messageContainer.textContent = String(event.data);
                }
            }
        });
    </script>

</body>
</html>


